# open_props_generator

Parses the open props css file to create a typesafe Gleam wrapper.

You probably don't want this dumb script unless you're planning to update the props
or fix a bug.

You probably just want the wrapper is published in [../open_props/](../open_props/)
